<?php

// No routes are defined here. The Filament panel is mounted at the root path
// (see AdminPanelProvider), so `/` is its dashboard — a route registered here
// for `/` would race the panel's own for the same path.
